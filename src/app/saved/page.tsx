import TaskList from "@/components/mainComponents/TaskList/TaskList"

const page = () => {

    return(
        <>
            <TaskList tagId={null} name={'کارهای ذخیره شده'} saved={true} />
        </>
    )
}

export default page